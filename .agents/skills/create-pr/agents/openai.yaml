interface:
  display_name: "Pull Request作成"
  short_description: "push済みの差分から簡潔な日本語Pull Requestを作成"
  default_prompt: "Use $create-pr to create a concise Japanese Pull Request from the pushed branch."
policy:
  allow_implicit_invocation: false
