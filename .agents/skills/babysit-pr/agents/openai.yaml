interface:
  display_name: "PRをオールグリーンまで完遂"
  short_description: "GHA監視からPRチェック収束まで完遂する手順"
  default_prompt: "Use $babysit-pr to commit and push the scoped changes, open a PR, monitor GHA, and fix failures with targeted local verification until all checks except VRTApprove pass."
policy:
  allow_implicit_invocation: false
