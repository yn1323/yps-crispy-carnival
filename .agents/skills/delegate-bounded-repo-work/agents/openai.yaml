interface:
  display_name: "定型リポジトリ作業を委譲"
  short_description: "定型調査と検証をLuna Maxへ安全に委譲するスキル"
  default_prompt: "Use $delegate-bounded-repo-work to delegate this bounded repository task to GPT-5.6 Luna Max."
policy:
  allow_implicit_invocation: true
