interface:
  display_name: "大規模リポジトリ作業を委譲"
  short_description: "大規模な調査と最終検証だけをLunaへ委譲するスキル"
  default_prompt: "Use $delegate-bounded-repo-work to decide whether this large bounded repository task should be delegated to GPT-5.6 Luna."
policy:
  allow_implicit_invocation: true
