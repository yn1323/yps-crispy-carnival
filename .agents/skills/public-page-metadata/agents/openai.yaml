interface:
  display_name: "公開ページのメタデータ変更"
  short_description: "公開ページのtitleと構造化データを同期漏れなく変更する"
  default_prompt: "Use $public-page-metadata to change Shiftori public page titles, descriptions, canonical URLs, OGP, or JSON-LD structured data without missing synchronized locations."
